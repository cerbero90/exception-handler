suites:
    main:
        spec_path: tests
        namespace: Cerbero\ExceptionHandler
        psr4_prefix: Cerbero\ExceptionHandler
extensions:
    - PhpSpec\Extension\CodeCoverageExtension
code_coverage:
    format:
      - clover
    output:
      clover: coverage.clover
